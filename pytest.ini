[pytest]
testpaths = tests
python_files = test_*.py
addopts = -v --tb=short --strict-markers
markers =
    concurrency: exercises real database locking with real threads
    security: asserts an attack does not work
    invariant: asserts a ledger invariant holds
filterwarnings =
    ignore::DeprecationWarning
